console.log("--- Login Status (Ternary) ---");

const isLoggedIn = true;

const statusMessage = isLoggedIn ? "Welcome back!" : "Please log in";

console.log("Login Status:", statusMessage);

const isLoggedIn2 = false;
const statusMessage2 = isLoggedIn2 ? "Welcome back!" : "Please log in";
console.log("Login Status:", statusMessage2);